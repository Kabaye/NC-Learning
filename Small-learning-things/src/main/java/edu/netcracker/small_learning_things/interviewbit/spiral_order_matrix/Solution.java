package edu.netcracker.small_learning_things.interviewbit.spiral_order_matrix;

import java.util.ArrayList;
import java.util.List;

public class Solution {
    // DO NOT MODIFY THE LIST. IT IS READ ONLY
    /*public ArrayList<Integer> spiralOrder(final List<ArrayList<Integer>> A) {
        int i = 0;
        int j = 0;
        int totalAmount = A.size() * A.get(0).size();
        List<Integer> result = new ArrayList<>(totalAmount);
        while (i + j < totalAmount) {
            result.add(A.get(i).get(j));
        }
    }*/
}

